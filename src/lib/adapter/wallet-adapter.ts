import IOTAWallet from '../vendor/iota/iota-wallet';

/**
 * Adapter class to provide map the api context to IOTA.
 *
 * Usage:
 * ```
 * import { Wallet } from 'grams-sdk';
 *
 * const wallet = new Wallet();
 * ```
 */
class WalletAdapter {}

export default WalletAdapter;
